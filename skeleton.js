'use strict'

// require deps

// SYNC init (like read conf file)

function skeleton() {
    var internal

    return {
        something: something
    }

    function privateStuff() {
        var stuff
        return stuff
    }

    function something(alfa) {
        return alfa
    }
}

module.exports = skeleton
